import { ValidateTenantMiddleware } from './validate-tenant.middleware';

describe('ValidateTenantMiddleware', () => {
  it('should be defined', () => {
    expect(ValidateTenantMiddleware).toBeDefined();
  });
});
